{
  "name": "helloworld",
  "version": "1.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "cordova-android": "^6.3.0"
  },
  "cordova": {
    "platforms": [
      "android"
    ]
  }
}